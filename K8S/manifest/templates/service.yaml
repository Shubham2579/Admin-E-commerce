apiVersion: v1
kind: Service
metadata:
  name: {{ include "admin-app.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "admin-app.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
